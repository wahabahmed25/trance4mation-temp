export type RoomSettings = {
    isAnonymous: boolean,
    maxParticipants: number,
    colorTheme: string,
    timeLimit: number,
    rounds: number,
    name: string,
    icon: string,
    description: string
}